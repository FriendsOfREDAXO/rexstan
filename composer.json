{
    "require": {
        "phpstan/phpstan": "^1.7",
        "phpstan/phpstan-symfony": "^1.2",
        "phpstan/phpstan-deprecation-rules": "^1.0",
        "phpstan/phpstan-strict-rules": "^1.2"
    },
    "autoload": {
        "classmap": [
            "lib/"
        ]
    },
    "config": {
        "bin-compat": "full"
    }
}
